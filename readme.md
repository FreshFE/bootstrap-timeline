# Single timeline for bootstrap

Single timeline layout

# Install

    {
        "dependencies": {
            "bootstrap-timeline": "~0.1.0"
        }
    }

    bower install

# Usage

    // must include bootstrap
    @import "bootstrap-path/bootstrap";
    @import "bootstrap-timeline-path/src/main.scss"